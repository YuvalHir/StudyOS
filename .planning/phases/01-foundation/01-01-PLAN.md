---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/api/src/services/auth.ts, apps/api/src/middleware/auth.ts, apps/api/src/middleware/tenant.ts, apps/api/src/index.ts]
autonomous: true
requirements: [M1-CORE-02, M1-CORE-03]
user_setup:
  - service: supabase
    why: "Token verification"
    env_vars:
      - name: SUPABASE_JWT_SECRET
        source: "Supabase Dashboard -> Project Settings -> API"

must_haves:
  truths:
    - "API rejects requests with invalid or missing tokens"
    - "Authenticated requests extract tenantId from JWT payload"
  artifacts:
    - path: "apps/api/src/services/auth.ts"
      provides: "JWT validation using hono/jwt"
    - path: "apps/api/src/middleware/tenant.ts"
      provides: "Extraction of tenant_id from app_metadata"
  key_links:
    - from: "apps/api/src/middleware/auth.ts"
      to: "apps/api/src/services/auth.ts"
      via: "token validation"
---

<objective>
Implement real Supabase Auth integration and secure tenant isolation middleware.

Purpose: Move from skeleton auth to real JWT verification and ensure every request is scoped to a tenant.
Output: Secure Auth/Tenant middleware stack in Hono.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@01-RESEARCH.md

<interfaces>
From apps/api/src/services/auth.ts:
```typescript
export type AuthUser = {
  id: string;
  email: string;
  tenantId: string;
};
export class AuthService {
  async validateToken(token: string): Promise<AuthUser | null>;
}
```

From apps/api/src/middleware/tenant.ts:
```typescript
export type TenantVariables = {
  tenantId: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement real JWT verification in AuthService</name>
  <files>apps/api/src/services/auth.ts</files>
  <action>
    - Update AuthService to use `hono/jwt`'s underlying verification or verify the JWT against `SUPABASE_JWT_SECRET`.
    - Extract `sub` (user_id) and `app_metadata.tenant_id` from the payload.
    - Map payload to `AuthUser` type.
    - Ensure it handles expired or malformed tokens gracefully by returning null.
  </action>
  <verify>
    <automated>npm run test -w apps/api -- --grep="AuthService"</automated>
  </verify>
  <done>AuthService validates real Supabase JWTs and returns user context with tenantId.</done>
</task>

<task type="auto">
  <name>Task 2: Update Auth and Tenant middleware</name>
  <files>apps/api/src/middleware/auth.ts, apps/api/src/middleware/tenant.ts</files>
  <action>
    - Refine `authMiddleware` to pass the token to `authService.validateToken`.
    - Refine `tenantMiddleware` to extract `tenantId` from the `user` variable set by `authMiddleware`.
    - Do NOT trust `x-tenant-id` header from client; always use the verified session data (per 01-RESEARCH.md).
  </action>
  <verify>
    <automated>npm run test -w apps/api -- --grep="middleware"</automated>
  </verify>
  <done>Middleware stack correctly authenticates users and isolates tenants using JWT claims.</done>
</task>

</tasks>

<verification>
Check that `/api/me` returns the user info when called with a valid Supabase JWT and 401 otherwise.
</verification>

<success_criteria>
- AuthService correctly parses Supabase JWTs.
- Tenant middleware extracts tenantId from JWT app_metadata.
- Unauthorized requests are rejected with 401.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
