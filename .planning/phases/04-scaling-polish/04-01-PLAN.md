---
phase: 04-scaling-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/app/layout.tsx, src/app/AppClient.tsx, src/app/globals.css, src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx]
autonomous: true
requirements: []
---

<objective>
Implement Dark Mode for the application.

Purpose: Provide a premium Apple-style dark mode experience with frosted glass and appropriate semantic colors.
Output: System-synced dark mode with manual toggle in sidebar.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-themes</name>
  <files>package.json</files>
  <action>
    - Install `next-themes`.
  </action>
  <verify>
    <automated>grep "next-themes" package.json</automated>
  </verify>
  <done>next-themes installed.</done>
</task>

<task type="auto">
  <name>Task 2: Configure ThemeProvider</name>
  <files>src/components/Providers/ThemeProvider.tsx, src/app/layout.tsx</files>
  <action>
    - Create ThemeProvider component using next-themes.
    - Wrap the root layout with ThemeProvider.
  </action>
  <verify>
    <automated>test -f src/components/Providers/ThemeProvider.tsx</automated>
  </verify>
  <done>ThemeProvider configured and integrated into layout.</done>
</task>

<task type="auto">
  <name>Task 3: Update globals.css for dark mode support</name>
  <files>src/app/globals.css</files>
  <action>
    - Update globals.css with dark mode variable overrides.
    - Add dark mode styles for .glass, .apple-card, .apple-input, etc.
  </action>
  <verify>
    <automated>grep "\.dark" src/app/globals.css</automated>
  </verify>
  <done>globals.css updated for dark mode.</done>
</task>

<task type="auto">
  <name>Task 4: Add Dark Mode Toggle in AppClient.tsx</name>
  <files>src/app/AppClient.tsx</files>
  <action>
    - Import useTheme from next-themes.
    - Add a button in the sidebar (AppClient.tsx) to toggle between light and dark themes.
    - Ensure icon/text updates based on current theme.
  </action>
  <verify>
    <automated>grep "useTheme" src/app/AppClient.tsx</automated>
  </verify>
  <done>Dark mode toggle added to sidebar.</done>
</task>

<task type="auto">
  <name>Task 5: Refine component dark mode styles</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx</files>
  <action>
    - Use dark: modifiers or CSS variables to ensure components look great in dark mode.
    - Refine text colors and background/border colors.
  </action>
  <verify>
    <automated>grep "dark:" src/components/ui/Button.tsx src/components/ui/Card.tsx src/components/ui/Input.tsx</automated>
  </verify>
  <done>Components refined for dark mode.</done>
</task>

</tasks>

<verification>
1. Run `npm run dev` and verify that the theme toggle works.
2. Check that the theme persists on reload (next-themes feature).
3. Ensure the UI looks correct in both light and dark modes.
</verification>

<success_criteria>
- Dark mode toggle button is visible and functional in the sidebar.
- Dark mode provides a high-quality "Apple" feel (dark glass, refined grays).
- Theme persistence is working.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scaling-polish/04-01-SUMMARY.md`
</output>
