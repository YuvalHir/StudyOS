---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [packages/db/src/schema.ts, packages/db/src/rls.ts]
autonomous: true
requirements: [M1-CORE-01]
user_setup:
  - service: supabase
    why: "Custom claims sync"
    dashboard_config:
      - task: "Create on_auth_user_created trigger"
        location: "Supabase Dashboard -> SQL Editor"
        action: "Run SQL from 01-RESEARCH.md Code Example 1"

must_haves:
  truths:
    - "RLS is enabled on all multi-tenant tables"
    - "Tenant isolation policy is applied to all multi-tenant tables"
  artifacts:
    - path: "packages/db/src/rls.ts"
      provides: "Drizzle-integrated setTenant and withTenant functions"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "packages/db/src/rls.ts"
      via: "withTenant wrapper"
---

<objective>
Enable Row-Level Security (RLS) in PostgreSQL and apply isolation policies.

Purpose: Enforce tenant isolation at the database level as a failsafe against application-layer bugs.
Output: PostgreSQL tables with active RLS and Drizzle-compatible RLS wrappers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@01-RESEARCH.md

<interfaces>
From packages/db/src/rls.ts:
```typescript
export function setTenant(tx: PgTransaction<any, any, any>, tenantId: string): Promise<void>;
export function withTenant<T>(db: any, tenantId: string, callback: (tx: any) => Promise<T>): Promise<T>;
export function getRLSSql(tableName: string): any[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate RLS migrations</name>
  <files>packages/db/drizzle/*.sql</files>
  <action>
    - Use `getRLSSql` utility to generate SQL that enables RLS and creates the tenant isolation policy for each multi-tenant table (`tenants`, `users`, `courses`, `schedule_slots`, `assignments`, `resources`).
    - Create a custom migration using `drizzle-kit generate` and add the RLS SQL to it.
    - Reference the "Pattern 2: RLS via Session Variables" from 01-RESEARCH.md.
  </action>
  <verify>
    <automated>ls packages/db/drizzle/*.sql | xargs grep "ENABLE ROW LEVEL SECURITY"</automated>
  </verify>
  <done>RLS policies are applied to all relevant tables in a migration file.</done>
</task>

<task type="auto">
  <name>Task 2: Refine withTenant wrapper for pool stability</name>
  <files>packages/db/src/rls.ts</files>
  <action>
    - Refine `withTenant` helper to use `is_local=true` in `set_config` (per 01-RESEARCH.md Pitfall 2).
    - Ensure it handles the PostgreSQL connection pool properly by executing inside a transaction.
  </action>
  <verify>
    <automated>npm run test -w packages/db -- --grep="RLS"</automated>
  </verify>
  <done>Robust withTenant helper available for the API layer.</done>
</task>

</tasks>

<verification>
Check `pg_policies` view in the database to confirm `tenant_isolation_policy` exists for all tables.
</verification>

<success_criteria>
- All tables have RLS enabled.
- `tenant_isolation_policy` uses `app.current_tenant` session variable.
- `withTenant` correctly sets the session variable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
