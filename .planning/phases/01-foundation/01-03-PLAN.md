---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: [apps/api/tests/integration.test.ts, packages/db/tests/rls.test.ts]
autonomous: true
requirements: [M1-CORE-04]

must_haves:
  truths:
    - "Integration tests confirm valid JWT allows access to tenant data"
    - "Integration tests confirm tenant A cannot see tenant B data"
  artifacts:
    - path: "apps/api/tests/integration.test.ts"
      provides: "End-to-end flow tests from request to DB"
---

<objective>
Verify authentication and tenant isolation with automated integration tests.

Purpose: Prove that the security measures are active and correctly preventing cross-tenant data access.
Output: Automated test suite for multi-tenant verification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@01-RESEARCH.md
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/phases/01-foundation/01-02-PLAN.md

<interfaces>
From apps/api/src/services/auth.ts:
```typescript
export class AuthService {
  async validateToken(token: string): Promise<AuthUser | null>;
}
```

From packages/db/src/rls.ts:
```typescript
export function withTenant<T>(db: any, tenantId: string, callback: (tx: any) => Promise<T>): Promise<T>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vitest and database test infrastructure</name>
  <files>package.json, apps/api/vitest.config.ts, packages/db/vitest.config.ts</files>
  <action>
    - Ensure Vitest is installed and configured for apps/api and packages/db.
    - Set up a test database environment (can use a separate schema or DB).
    - Create a test setup that seeds two tenants and one user for each.
  </action>
  <verify>
    <automated>npm run test -w apps/api -- --version</automated>
  </verify>
  <done>Testing infrastructure is ready for integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant isolation integration test</name>
  <files>apps/api/tests/integration.test.ts</files>
  <action>
    - Write an integration test that:
      1. Mocks a Supabase JWT for Tenant A.
      2. Inserts data for Tenant A.
      3. Requests Tenant A data (should succeed).
      4. Mocks a Supabase JWT for Tenant B.
      5. Requests Tenant A data using Tenant B's session (should return empty or 401/403/404).
      6. Requests Tenant B data (should succeed).
    - Use `withTenant` wrapper in the test to verify RLS at the DB level.
  </action>
  <verify>
    <automated>npm run test -w apps/api -- tests/integration.test.ts</automated>
  </verify>
  <done>Integration tests prove tenant isolation is active across the stack.</done>
</task>

</tasks>

<verification>
Run all integration tests and ensure they pass with RLS enabled.
</verification>

<success_criteria>
- Auth extraction from JWT is tested and verified.
- RLS isolation between two different tenants is proven by tests.
- Automated tests pass in CI-ready environment.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
